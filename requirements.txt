streamlit>=1.32
pypdf>=4.0
python-docx>=1.1
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
plotly>=5.20
reportlab>=4.1
requests>=2.31
python-dotenv>=1.0
pytest>=8.0

# Advanced/optional features
spacy>=3.8,<3.9
sentence-transformers>=2.7
# Streamlit Cloud only runs `pip install -r requirements.txt` - it can't run
# `python -m spacy download`, so the model is installed as a direct wheel.
# Version pinned to match the spacy version above (spaCy models must match
# the spaCy minor version they were built for).
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
